artifact_dir: "C:\\Users\\samue\\projects\\dev_tools\\test_bin"
components:
  - name: 'tp'
    location: 'tp_git'
    build:
      artifacts:
        - name: "tp_test.exe"
        - name: "tp_test"
      commands:
        - exec: "go build -o tp_test ."
          windows: "go build -o tp_test.exe ."
  - name: 'bldr'
    location: 'bldr'
    build:
      artifacts:
        - name: "bldr_test.exe"
        - name: "bldr_test"
      commands:
        - exec: "go build -o bldr_test ."
          windows: "go build -o bldr_test.exe ."
  - name: 'pf'
    location: 'p_finder'
    build:
      artifacts:
        - name: "pf_test.exe"
        - name: "pf_test"
      commands:
        - exec: "go build -o pf_test ."
          windows: "go build -o pf_test.exe ."
  - name: 'go htp'
    location: 'htp'
    build:
      artifacts:
        - name: "htp_test.exe"
        - name: "htp_test"
      commands:
        - exec: "go build -o htp_test ."
          windows: "go build -o htp_test.exe ."
  - name: 'go proxy'
    location: 'go_proxy'
    build:
      artifacts:
        - name: "proxy_test.exe"
        - name: "proxy_test"
      commands:
        - exec: "go build -o proxy_test ."
          windows: "go build -o proxy_test.exe ."
  - name: 'cli watcher'
    location: 'cli_watcher'
    build:
      artifacts:
        - name: "cliw_test.exe"
        - name: "cliw_test"
      commands:
        - exec: "go build -o cliw_test ."
          windows: "go build -o cliw_test.exe ."