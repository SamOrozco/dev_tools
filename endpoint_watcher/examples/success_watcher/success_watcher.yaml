name: 'top-level'
endpoint:
  url: 'https://google.com'
  method: 'GET'
cond:
  type: 'script'
  javascript: >
    var code = statusCode;
    var body = responseBody;
    var def = code === 200;
success:
  - type: 'watcher'
    watcher:
      name: 'second-level'
      endpoint:
        url: 'https://google.com'
        method: 'GET'
      cond:
        type: 'script'
        javascript: >
          var code = statusCode;
          var body = responseBody;
          var def = code === 200;
      success:
        - message: 'finished watcher'