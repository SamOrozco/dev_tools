artifact_dir: "/Users/samuelorozco/bin"
components:
  - name: 'tp'
    location: 'tp_git'
    build:
      artifacts:
        - name: "tp.exe"
        - name: "tp"
      commands:
        - exec: "go build -o tp ."
          windows: "go build -o tp.exe ."
  - name: 'bldr'
    location: 'bldr'
    build:
      artifacts:
        - name: "bldr.exe"
        - name: "bldr"
      commands:
        - exec: "go build -o bldr ."
          windows: "go build -o bldr.exe ."
  - name: 'pf'
    location: 'p_finder'
    build:
      artifacts:
        - name: "pf.exe"
        - name: "pf"
      commands:
        - exec: "go build -o pf ."
          windows: "go build -o pf.exe ."
  - name: 'go htp'
    location: 'htp'
    build:
      artifacts:
        - name: "htp.exe"
        - name: "htp"
      commands:
        - exec: "go build -o htp ."
          windows: "go build -o htp.exe ."
  - name: 'go proxy'
    location: 'go_proxy'
    build:
      artifacts:
        - name: "proxy.exe"
        - name: "proxy"
      commands:
        - exec: "go build -o proxy ."
          windows: "go build -o proxy.exe ."
  - name: 'cliw'
    location: 'cli_watcher'
    build:
      artifacts:
        - name: "cliw.exe"
        - name: "cliw"
      commands:
        - exec: "go build -o cliw ."
          windows: "go build -o cliw.exe ."
  - name: 'jit'
    location: 'jit'
    build:
      artifacts:
        - name: "jit.exe"
        - name: "jit"
      commands:
        - exec: "go build -o jit ."
          windows: "go build -o jit.exe ."
  - name: 'ss'
    location: 'sql_star'
    build:
      artifacts:
        - name: "ss.exe"
        - name: "ss"
      commands:
        - exec: "go build -o ss ."
          windows: "go build -o ss.exe ."
  - name: 'ts'
    location: 'time_series'
    build:
      artifacts:
        - name: "ts.exe"
        - name: "ts"
      commands:
        - exec: "go build -o ts ."
          windows: "go build -o ts.exe ."
  - name: 'remind'
    location: 'simple_reminder'
    build:
      artifacts:
        - name: "remind.exe"
        - name: "remind"
      commands:
        - exec: "go build -o remind ."
          windows: "go build -o remind.exe ."